Estoy haciendo lo basico
1)git ini     // Se utiliza para inicializar la carpeta .git que va a contener las versiones
2)git add 'archivo'   // Se utiliza para anadir el archivo al control de versiones.
3)gid commit -m "Descripcion Cambios"  // Recordar hacer los pasos 2 y 3 por cada version

Para usar Github primero creamos nuevo proyecto de preferencia con el mismo nombre que la carpeta luego
4)git remote add 'alias' htts://github.com/cuenta/carpeta.git (Solo 1 vez)
Finalmente push
5)git push -u 'alias' 'rama'  // el alias puesto en el paso 4 y la rama por lo general master (ojo sin comillas)
///////////////////////////////////////////////
6) cambie algo